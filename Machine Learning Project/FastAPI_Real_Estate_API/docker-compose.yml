version: '3.8'

services:
  realestate-app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      - ./backend/Dataset:/app/backend/Dataset
    restart: unless-stopped
